node_modules
dist
build
coverage
logs
package-lock.json
pnpm-lock.yaml

# Generated as minified JSON, and committed. The pre-commit prettier glob
# covers *.json, so without these entries they would be pretty-printed on every
# commit that touches them — inflating multi-MB blobs and then losing to the
# next `pnpm build:lexicon` / `pnpm fetch:embeddings`, which rewrite them
# minified. Generated output must not be formatted.
src/assets/semantic-vectors.json
scripts/lexicon-embeddings.json