node_modules
dist
build
coverage
logs
package-lock.json
pnpm-lock.yaml

# Generated by scripts/build-lexicon.js as minified JSON, and committed. The
# pre-commit prettier glob covers *.json, so without this it would be
# pretty-printed on every commit that touches it — inflating a 150 KB numeric
# blob and then losing to the next `pnpm build:lexicon`, which rewrites it
# minified. Generated output must not be formatted.
src/assets/semantic-vectors.json